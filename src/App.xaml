<Application x:Class="SpartanShield.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SpartanShield"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="Secondary" Color="#D90429"/>
        <SolidColorBrush x:Key="Background" Color="#EDF2F4"/>
        <SolidColorBrush x:Key="Primary" Color="#EF233C" />
        <SolidColorBrush x:Key="Secure" Color="#00b200"/>
        <SolidColorBrush x:Key="Insecure" Color="#ff0000"/>
        <Style x:Key="TransparentStyle" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
